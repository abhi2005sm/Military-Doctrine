import { CompareClient } from '../../components/catalog/CompareClient';

export const metadata = {
  title: 'Asset Comparison Matrix — Blackstone',
  description: 'Side-by-side specs and quantitative 8-criteria rating matrix for comparing global military assets.',
};

export default function ComparePage() {
  return <CompareClient />;
}
